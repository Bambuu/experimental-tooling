"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/BlendingCandidatesController');

describe('controllers:BlendingCandidatesController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
