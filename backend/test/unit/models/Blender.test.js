"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Blender');
const Fixture = require('../../fixtures/BlenderFixture');

describe('models:Blender', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
