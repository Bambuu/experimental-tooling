"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/BlendingCandidates');
const Fixture = require('../../fixtures/BlendingCandidatesFixture');

describe('models:BlendingCandidates', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
